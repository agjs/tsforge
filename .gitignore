node_modules/
dist/
.astro/
# Ignore everything under evals/ (run output, scratch) EXCEPT the committed
# corpus seeds, so `eval:sweep` has a versioned set of tasks to run against.
evals/*
!evals/corpus/
*.log
.DS_Store
.env
.env.*
!.env.example
.claude/
evals/runs/
evals/.web-cache-*/
.tsforge/
models.json

# Generated gate artifact + stray scaffold left at the repo root from running
# tsforge against its own directory during development (the real source lives in
# packages/core/ and apps/docs/). Anchored to root so packages/core/src etc. are unaffected.
/tsforge.tsconfig.json
/index.html
/vite.config.ts
/components.json
/src/
