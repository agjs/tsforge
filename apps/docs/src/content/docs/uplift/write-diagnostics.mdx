---
title: Write diagnostics
description: Type errors shown immediately after each edit, before the full gate runs.
---

The **session gate** runs when tsforge decides a task is finished. That can take a while on a big repo.

**Write diagnostics** are faster feedback: after every successful `edit` or `create`, tsforge typechecks **just that file** and appends any errors to the tool result. The model can fix mistakes before the next full gate run.

Always on.

## What it looks like

```
✓ edited src/foo.ts

Type diagnostics (src/foo.ts):
  L12:5 TS2322: Type 'string' is not assignable to type 'number'.
  …and 3 more
```

At most **5** errors are shown; extra ones are summarized as `…and N more`.

Write diagnostics do not replace the gate. They shorten the feedback loop.

→ [How the gate is built](/loop/gate-floor/) · [TypeScript language server](/lsp/typescript-server/) · [When the gate fails](/loop/validation/)
