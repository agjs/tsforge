# tsforge v0.1.0 Release Prep + Docs Rewrite

Validated against the repo on branch `feature/guardrail-packs-local-uplift` (680 tests: 669 pass, 11 skip, 0 fail; `bun run validate` green).

**Owner decisions (locked in):**

- LICENSE copyright: **Aleksandar Grbic**
- Root `package.json`: remove `"private": true` (npm-publishable later); keep `packages/core/package.json` private until a separate publish story exists
- README badges: **https://github.com/agjs/tsforge** (adjust if the final org/repo name differs at D3)

---

## Phase A — Release hygiene

One commit per task. Run `bun run validate` after each.

### A1 — Remove LAN-IP default

- Change `PROVIDER_DEFAULTS.baseUrl` from `http://192.168.20.107:8000/v1` → `http://localhost:8000/v1`
- Update `banner.test.ts` fixture endpoint to `localhost:8000`
- **DONE:** `grep -rn "192\.168" packages apps docs *.json` → 0 hits outside `bun.lock`

### A2 — Delete internal scratch

Delete: `CLEANUP_PLAN.md`, `CLI_PLAN.md`, `docs/AHMAD_AWAIS_TWITTER.md`, `docs/NOTES.md`

### A3 — LICENSE + package metadata

- Add `LICENSE` — MIT, Copyright (c) 2026 Aleksandar Grbic
- Root `package.json`: `"license": "MIT"`, remove `"private": true`, bump `"version"` to `"0.1.0"`
- `packages/core/package.json`: `"license": "MIT"`, bump version to `"0.1.0"` (keep `"private": true`)

### A4 — Scrub + gitignore

- Extend `.gitignore`: `.tsforge/`, `models.json`

### A5 — Skip comments

- `browser-oracle.test.ts` line 51: add why-comment
- Document env-gated skips in CONTRIBUTING (B3)

### A6 — Regenerate rule docs

```bash
bun packages/core/scripts/build-rule-docs.ts
bun packages/core/scripts/build-rules-md.ts
```

---

## Phase B — GitHub markdown (boringstack style)

See plan for B1–B5 content specs (README, AGENTS, CONTRIBUTING, SECURITY, ROADMAP).

---

## Phase C — Docs site (apps/docs/)

See plan for C1–C6 (index, guardrails, uplift, eval, flags, stale pages, sidebar).

---

## Phase D — Ship

| Task | Action | DONE when |
| ---- | ------ | --------- |
| D1 | `bun run validate` + docs build + regen (no diff) | all green |
| D2 | Merge → main (no-ff); tag `v0.1.0` | tag on main |
| D3 | **STOP** — do not push or publish | owner notified |

---

## Hard constraints

- **Code changes limited to:** A1 constant, A5 comment, A3/A4 metadata/gitignore only
- Do NOT weaken tests, touch `eslint.config.js`, or refactor `packages/core/src/**`
- Conventional commits per task
